# Square Calendar Sink

This plugin owns the Square calendar sink workflow. It is intentionally separate
from time card management so schedule synchronization can be installed,
described, and evolved independently.
